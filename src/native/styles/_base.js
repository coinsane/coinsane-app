import variables from './_variables';
import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  white: { color: variables.colors.white },
  textCenter: { textAlign: 'center' },
});