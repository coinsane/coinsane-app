import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  period: {
    flex: 1,
    flexDirection: 'row',
    justifyContent: 'space-around',
    alignItems: 'center',
    flexWrap: 'nowrap',
  },
  buttonContainer: {
    marginLeft: 15,
    marginRight: 15,
  },
});
